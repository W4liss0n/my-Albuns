{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dialog",
  "description": "Minimum permissions for the owned standard message dialog",
  "local": true,
  "windows": ["dialog-project-failure"],
  "permissions": [
    "message-dialog-window-commands",
    "core:window:allow-start-dragging"
  ]
}
